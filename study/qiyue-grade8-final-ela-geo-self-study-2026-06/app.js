const STORE_KEY='qiyue-ela-geo-self-study-v1';let current=null;let revealed=false;const $=id=>document.getElementById(id);const state=loadState();function loadState(){try{return JSON.parse(localStorage.getItem(STORE_KEY))||{answered:0,correct:0,wrong:{},seen:{}}}catch{return{answered:0,correct:0,wrong:{},seen:{}}}}function save(){localStorage.setItem(STORE_KEY,JSON.stringify(state));renderStats();renderWrongList()}function unique(arr){return [...new Set(arr)].sort((a,b)=>a.localeCompare(b,'zh-Hant'))}function initControls(){const subjects=['全部科目',...unique(QUESTIONS.map(q=>q.subject))];$('subjectSelect').innerHTML=subjects.map(s=>`<option value="${s==='全部科目'?'':s}">${s}</option>`).join('');renderTypeOptions();$('subjectSelect').addEventListener('change',()=>{renderTypeOptions();pickQuestion()});$('typeSelect').addEventListener('change',pickQuestion);$('modeSelect').addEventListener('change',pickQuestion);$('newQuestion').addEventListener('click',pickQuestion);$('revealAnswer').addEventListener('click',reveal);$('markCorrect').addEventListener('click',()=>mark(true));$('markWrong').addEventListener('click',()=>mark(false));$('resetStats').addEventListener('click',()=>{if(confirm('確定清除本機統計與錯題紀錄？')){state.answered=0;state.correct=0;state.wrong={};state.seen={};save();pickQuestion()}})}function renderTypeOptions(){const subj=$('subjectSelect').value;const list=QUESTIONS.filter(q=>!subj||q.subject===subj);const types=['全部題型',...unique(list.map(q=>q.type))];$('typeSelect').innerHTML=types.map(t=>`<option value="${t==='全部題型'?'':t}">${t}</option>`).join('')}function filtered(){const subj=$('subjectSelect').value,type=$('typeSelect').value,mode=$('modeSelect').value;let list=QUESTIONS.filter(q=>(!subj||q.subject===subj)&&(!type||q.type===type));if(mode==='wrong'){const ids=new Set(Object.keys(state.wrong).filter(id=>state.wrong[id]>0));list=list.filter(q=>ids.has(q.id));}return list}function pickQuestion(){const list=filtered();if(!list.length){current=null;revealed=false;$('qSubject').textContent='沒有題目';$('qType').textContent='';$('qSkill').textContent='';$('qStem').textContent=$('modeSelect').value==='wrong'?'目前沒有符合條件的錯題。先回全部題庫做幾題。':'目前篩選沒有題目，請換科目或題型。';$('choices').innerHTML='';$('answerPanel').hidden=true;toggleMark(false);return}current=list[Math.floor(Math.random()*list.length)];revealed=false;state.seen[current.id]=(state.seen[current.id]||0)+1;localStorage.setItem(STORE_KEY,JSON.stringify(state));$('qSubject').textContent=current.subject;$('qType').textContent=current.type;$('qSkill').textContent=current.skill||'自學練習';$('qStem').textContent=current.stem;$('choices').innerHTML=(current.choices||[]).map(c=>`<div class="choice"><b>${escapeHtml(c.key)}</b>${escapeHtml(c.text)}</div>`).join('')||'<div class="choice">這題是翻卡題：先在心裡或紙上寫答案，再翻答案。</div>';$('answerPanel').hidden=true;$('qAnswer').textContent='';$('qExplanation').textContent='';$('qSource').textContent='';toggleMark(false)}function reveal(){if(!current)return;revealed=true;$('answerPanel').hidden=false;$('qAnswer').textContent=current.answer;$('qExplanation').textContent=current.explanation;$('qSource').textContent=current.source;toggleMark(true)}function toggleMark(on){$('markCorrect').disabled=!on;$('markWrong').disabled=!on}function mark(ok){if(!current||!revealed)return;state.answered++;if(ok){state.correct++;if(state.wrong[current.id])state.wrong[current.id]=Math.max(0,state.wrong[current.id]-1)}else{state.wrong[current.id]=(state.wrong[current.id]||0)+1}save();pickQuestion()}function renderStats(){const wrong=Object.values(state.wrong).reduce((a,b)=>a+(b>0?b:0),0);$('statAnswered').textContent=state.answered;$('statCorrect').textContent=state.correct;$('statWrong').textContent=wrong;$('statRate').textContent=state.answered?Math.round(state.correct/state.answered*100)+'%':'0%'}function renderWrongList(){const entries=Object.entries(state.wrong).filter(([,n])=>n>0).sort((a,b)=>b[1]-a[1]).slice(0,24);$('wrongList').innerHTML=entries.length?entries.map(([id,n])=>{const q=QUESTIONS.find(x=>x.id===id);return `<span class="wrong-pill">${escapeHtml(q?`${q.subject}｜${q.type}`:id)} ×${n}</span>`}).join(''):'<span class="wrong-pill">目前沒有錯題，先做 10 題暖身。</span>'}function escapeHtml(s){return String(s||'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]))}initControls();renderStats();renderWrongList();pickQuestion();
